{
  "name": "nginx-datadog-v2-fargate",
  "description": "Nginx with Datadog autodiscovery v2 on EKS Fargate (sidecar injection)",
  "type": "kubectl",
  "built_in": true,
  "deploy_commands": [
    "kubectl create namespace fargate --dry-run=client -o yaml | kubectl apply -f -",
    "kubectl apply -f deployment.yaml"
  ],
  "update_commands": [
    "kubectl apply -f deployment.yaml"
  ],
  "undeploy_commands": [
    "kubectl delete -f deployment.yaml --ignore-not-found"
  ],
  "files": ["deployment.yaml"]
}
