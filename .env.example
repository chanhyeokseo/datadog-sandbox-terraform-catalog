# ==========================================
# AWS Credentials
# ==========================================
# The container mounts ~/.aws from your host (read-only).
# If you have AWS CLI configured (SSO, profiles, etc.),
# credentials are resolved automatically via the standard chain.
#
# To use a specific profile:
AWS_PROFILE=my-profile
#
# To override with explicit credentials (not recommended):
# AWS_ACCESS_KEY_ID=your-access-key-id
# AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_SESSION_TOKEN=your-session-token

# ==========================================
# Identity Salt (REQUIRED)
# ==========================================
# Stable identifier used for naming S3 buckets, DynamoDB tables, and Parameter Store paths.
# Must be unique per user. Once set, do not change it.
DOGSTAC_SALT=your-unique-salt-value

# ==========================================
# Optional Configuration
# ==========================================
# TERRAFORM_DATA_PATH=./terraform-data
# FORCE_REINIT=false
