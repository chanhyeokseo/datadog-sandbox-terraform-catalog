# ==========================================
# AWS Credentials (REQUIRED)
# ==========================================
# IMPORTANT: AWS credentials MUST be set via environment variables
# to enable configuration persistence in Parameter Store.
# The container loads saved config from Parameter Store on startup,
# which requires AWS credentials to be available.
# ==========================================

AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
# AWS_SESSION_TOKEN=  # Optional, for temporary credentials
AWS_REGION=ap-northeast-2

# ==========================================
# Backend Configuration
# ==========================================
USE_PARAMETER_STORE=true
TERRAFORM_DIR=/app/terraform

# ==========================================
# Persistent Storage Configuration
# ==========================================
# Path for persistent Terraform data (instances, modules, state, keys)
# Default: ./terraform-data (auto-created by Docker, no setup needed)
# For system-wide storage, use absolute path (requires manual creation first):
#   sudo mkdir -p /etc/dogstac && sudo chown -R $USER:$USER /etc/dogstac
#   Then set: TERRAFORM_DATA_PATH=/etc/dogstac
# TERRAFORM_DATA_PATH=./terraform-data

# Force re-initialization on next startup (clears instances/modules and re-clones from Git)
# Set to true to fetch fresh data from the repository
# FORCE_REINIT=false

# ==========================================
# Instances Repository (REQUIRED)
# ==========================================
# Git repository URL containing Terraform instance templates
INSTANCES_REPO_URL=https://github.com/chanhyeokseo/dogstac.git

# Branch to use for instances and modules (defaults to 'webui')
INSTANCES_REPO_BRANCH=webui
