RDS Database Instance (multi-engine).

- **Supported Engines:** PostgreSQL, MySQL, Oracle, SQL Server, DocumentDB
- **Configuration:** Set `rds_type` variable to choose engine (postgres, mysql, oracle, sqlserver, docdb)
